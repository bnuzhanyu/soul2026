{
  "scripts": {
    "dev": "vite --config frontend/vite.config.js",
    "build": "vite build --config frontend/vite.config.js",
    "preview": "vite preview --config frontend/vite.config.js --host 127.0.0.1",
    "dev:api": "UV_CACHE_DIR=.uv-cache uv run uvicorn backend.jomo_backend.app:app --host 127.0.0.1 --port 4174 --reload"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}

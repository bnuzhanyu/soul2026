[project]
name = "jomo-demo"
version = "0.1.0"
description = "JOMO AI skill companion demo backend agent."
requires-python = ">=3.11"
dependencies = [
  "claude-agent-sdk>=0.2.87",
  "starlette>=0.37.2",
  "uvicorn>=0.30.0",
  "volcengine-python-sdk[ark]>=5.0.35",
]

[tool.uv]
package = false
